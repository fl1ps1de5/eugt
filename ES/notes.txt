_dof_states = gym.acquire_dof_state_tensor(sim)
dof_states = gymtorch.wrap_tensor(_dof_states)

# fill dof_states with [num_dofs, 2] things
# this means each DOF has 2 values, namely position and velocity

gym.refresh_dof_state_tensor(sim)

# this updates the value that is actuall in dof_states somehow


